"""提示词管理模块

集中管理所有AI提示词，便于统一修改和维护
"""

# 威胁类型配置
THREAT_LABELS = {
    "正常流量": "normal",
    "SQL注入": "sqli",
    "XSS攻击": "xss",
    "其他可疑": "other"
}

# 标签别名映射
LABEL_ALIASES = {
    "正常": "正常流量",
    "无威胁": "正常流量",
    "安全": "正常流量",
    "SQL": "SQL注入",
    "注入": "SQL注入",
    "XSS": "XSS攻击",
    "跨站": "XSS攻击",
    "脚本攻击": "XSS攻击",
    "可疑": "其他可疑",
    "恶意": "其他可疑"
}

# AI分析提示词
ANALYSIS_PROMPT = """
你是一个网络安全专家。请分析以下 HTTP 流量日志片段。

**重要要求**：
请在回答的**第一行**，用半角方括号列出检测到的所有威胁类型，多个类型用逗号分隔。
可选类型：正常流量, SQL注入, XSS攻击, 其他可疑

**关键约束**：
- 只标记日志中**确实出现**的攻击特征，不要根据经验或可能性进行推测。
- 例如：如果日志中只包含 `<script>` 标签而无 SQL 语句，则**只能**标记 `[XSS攻击]`，绝对不要同时标记 `[SQL注入]`。

**置信度评分**：
请在第二行，为每个检测到的威胁类型提供 0-100 的置信度评分，格式为：
置信度: 威胁类型1=评分1, 威胁类型2=评分2

格式示例：
[正常流量]
置信度: 正常流量=95
[SQL注入, XSS攻击]
置信度: SQL注入=85, XSS攻击=90
[其他可疑]
置信度: 其他可疑=70

然后从第三行开始，用中文详细解释分析理由。

流量日志如下：
{content_for_ai}
"""

# 初始化风险计数函数
def init_risk_counts():
    """初始化风险计数字典"""
    return {key: 0 for key in THREAT_LABELS.keys()}
